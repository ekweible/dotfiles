# SSH config managed by chezmoi
# Actual keys are per-machine and NOT managed by chezmoi (see .chezmoiignore)

{{ if eq .profile "work" -}}
# Work GitHub (default for health checks and .chezmoiexternal.toml)
Host github.com
    HostName github.com
    IdentityFile /Users/evanweible/.ssh/evanweible-wf.id_ed25519
    User git
    AddKeysToAgent yes
    UseKeychain yes

# Personal GitHub (used via git URL rewriting for personal repos)
Host github.com-personal
    HostName github.com
    IdentityFile /Users/evanweible/.ssh/ekweible.id_ed25519
    User git
    AddKeysToAgent yes
    UseKeychain yes
{{- else -}}
# Personal GitHub (default)
Host github.com
    HostName github.com
    IdentityFile /Users/evanweible/.ssh/ekweible.id_ed25519
    User git
    AddKeysToAgent yes
    UseKeychain yes
{{- end }}

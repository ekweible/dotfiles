#!/bin/bash
{{ if eq .profile "work" -}}
# Run work-private installation scripts
WORK_DIR="${HOME}/.local/share/chezmoi-work"

if [ ! -d "${WORK_DIR}" ]; then
    exit 0
fi

# Execute work installation script if it exists
if [ -f "${WORK_DIR}/install-wk.sh" ]; then
    echo "🔧 Installing work tools..."
    bash "${WORK_DIR}/install-wk.sh"
fi
{{- end }}
